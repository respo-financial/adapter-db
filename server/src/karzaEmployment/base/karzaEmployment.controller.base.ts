/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { KarzaEmploymentService } from "../karzaEmployment.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { KarzaEmploymentCreateInput } from "./KarzaEmploymentCreateInput";
import { KarzaEmploymentWhereInput } from "./KarzaEmploymentWhereInput";
import { KarzaEmploymentWhereUniqueInput } from "./KarzaEmploymentWhereUniqueInput";
import { KarzaEmploymentFindManyArgs } from "./KarzaEmploymentFindManyArgs";
import { KarzaEmploymentUpdateInput } from "./KarzaEmploymentUpdateInput";
import { KarzaEmployment } from "./KarzaEmployment";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class KarzaEmploymentControllerBase {
  constructor(
    protected readonly service: KarzaEmploymentService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: KarzaEmployment })
  @nestAccessControl.UseRoles({
    resource: "KarzaEmployment",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(
    @common.Body() data: KarzaEmploymentCreateInput
  ): Promise<KarzaEmployment> {
    return await this.service.create({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        reqPayload: true,
        resPayload: true,
        customerId: true,
        errorCode: true,
        errorMessage: true,
        httpStatusCode: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [KarzaEmployment] })
  @ApiNestedQuery(KarzaEmploymentFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "KarzaEmployment",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<KarzaEmployment[]> {
    const args = plainToClass(KarzaEmploymentFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        reqPayload: true,
        resPayload: true,
        customerId: true,
        errorCode: true,
        errorMessage: true,
        httpStatusCode: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: KarzaEmployment })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "KarzaEmployment",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: KarzaEmploymentWhereUniqueInput
  ): Promise<KarzaEmployment | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        reqPayload: true,
        resPayload: true,
        customerId: true,
        errorCode: true,
        errorMessage: true,
        httpStatusCode: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: KarzaEmployment })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "KarzaEmployment",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: KarzaEmploymentWhereUniqueInput,
    @common.Body() data: KarzaEmploymentUpdateInput
  ): Promise<KarzaEmployment | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          reqPayload: true,
          resPayload: true,
          customerId: true,
          errorCode: true,
          errorMessage: true,
          httpStatusCode: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: KarzaEmployment })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "KarzaEmployment",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: KarzaEmploymentWhereUniqueInput
  ): Promise<KarzaEmployment | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          reqPayload: true,
          resPayload: true,
          customerId: true,
          errorCode: true,
          errorMessage: true,
          httpStatusCode: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
