/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { HyperVergeOtpService } from "../hyperVergeOtp.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { HyperVergeOtpCreateInput } from "./HyperVergeOtpCreateInput";
import { HyperVergeOtpWhereInput } from "./HyperVergeOtpWhereInput";
import { HyperVergeOtpWhereUniqueInput } from "./HyperVergeOtpWhereUniqueInput";
import { HyperVergeOtpFindManyArgs } from "./HyperVergeOtpFindManyArgs";
import { HyperVergeOtpUpdateInput } from "./HyperVergeOtpUpdateInput";
import { HyperVergeOtp } from "./HyperVergeOtp";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class HyperVergeOtpControllerBase {
  constructor(
    protected readonly service: HyperVergeOtpService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: HyperVergeOtp })
  @nestAccessControl.UseRoles({
    resource: "HyperVergeOtp",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(
    @common.Body() data: HyperVergeOtpCreateInput
  ): Promise<HyperVergeOtp> {
    return await this.service.create({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        customerId: true,
        reqPayload: true,
        resPayload: true,
        httpStatusCode: true,
        errorCode: true,
        errorMessage: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [HyperVergeOtp] })
  @ApiNestedQuery(HyperVergeOtpFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "HyperVergeOtp",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<HyperVergeOtp[]> {
    const args = plainToClass(HyperVergeOtpFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        customerId: true,
        reqPayload: true,
        resPayload: true,
        httpStatusCode: true,
        errorCode: true,
        errorMessage: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: HyperVergeOtp })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "HyperVergeOtp",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: HyperVergeOtpWhereUniqueInput
  ): Promise<HyperVergeOtp | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        customerId: true,
        reqPayload: true,
        resPayload: true,
        httpStatusCode: true,
        errorCode: true,
        errorMessage: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: HyperVergeOtp })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "HyperVergeOtp",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: HyperVergeOtpWhereUniqueInput,
    @common.Body() data: HyperVergeOtpUpdateInput
  ): Promise<HyperVergeOtp | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          customerId: true,
          reqPayload: true,
          resPayload: true,
          httpStatusCode: true,
          errorCode: true,
          errorMessage: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: HyperVergeOtp })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "HyperVergeOtp",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: HyperVergeOtpWhereUniqueInput
  ): Promise<HyperVergeOtp | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          customerId: true,
          reqPayload: true,
          resPayload: true,
          httpStatusCode: true,
          errorCode: true,
          errorMessage: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
