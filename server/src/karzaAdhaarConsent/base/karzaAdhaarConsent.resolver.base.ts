/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { CreateKarzaAdhaarConsentArgs } from "./CreateKarzaAdhaarConsentArgs";
import { UpdateKarzaAdhaarConsentArgs } from "./UpdateKarzaAdhaarConsentArgs";
import { DeleteKarzaAdhaarConsentArgs } from "./DeleteKarzaAdhaarConsentArgs";
import { KarzaAdhaarConsentFindManyArgs } from "./KarzaAdhaarConsentFindManyArgs";
import { KarzaAdhaarConsentFindUniqueArgs } from "./KarzaAdhaarConsentFindUniqueArgs";
import { KarzaAdhaarConsent } from "./KarzaAdhaarConsent";
import { KarzaAdhaarConsentService } from "../karzaAdhaarConsent.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => KarzaAdhaarConsent)
export class KarzaAdhaarConsentResolverBase {
  constructor(
    protected readonly service: KarzaAdhaarConsentService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "KarzaAdhaarConsent",
    action: "read",
    possession: "any",
  })
  async _karzaAdhaarConsentsMeta(
    @graphql.Args() args: KarzaAdhaarConsentFindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [KarzaAdhaarConsent])
  @nestAccessControl.UseRoles({
    resource: "KarzaAdhaarConsent",
    action: "read",
    possession: "any",
  })
  async karzaAdhaarConsents(
    @graphql.Args() args: KarzaAdhaarConsentFindManyArgs
  ): Promise<KarzaAdhaarConsent[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => KarzaAdhaarConsent, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "KarzaAdhaarConsent",
    action: "read",
    possession: "own",
  })
  async karzaAdhaarConsent(
    @graphql.Args() args: KarzaAdhaarConsentFindUniqueArgs
  ): Promise<KarzaAdhaarConsent | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => KarzaAdhaarConsent)
  @nestAccessControl.UseRoles({
    resource: "KarzaAdhaarConsent",
    action: "create",
    possession: "any",
  })
  async createKarzaAdhaarConsent(
    @graphql.Args() args: CreateKarzaAdhaarConsentArgs
  ): Promise<KarzaAdhaarConsent> {
    return await this.service.create({
      ...args,
      data: args.data,
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => KarzaAdhaarConsent)
  @nestAccessControl.UseRoles({
    resource: "KarzaAdhaarConsent",
    action: "update",
    possession: "any",
  })
  async updateKarzaAdhaarConsent(
    @graphql.Args() args: UpdateKarzaAdhaarConsentArgs
  ): Promise<KarzaAdhaarConsent | null> {
    try {
      return await this.service.update({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => KarzaAdhaarConsent)
  @nestAccessControl.UseRoles({
    resource: "KarzaAdhaarConsent",
    action: "delete",
    possession: "any",
  })
  async deleteKarzaAdhaarConsent(
    @graphql.Args() args: DeleteKarzaAdhaarConsentArgs
  ): Promise<KarzaAdhaarConsent | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
