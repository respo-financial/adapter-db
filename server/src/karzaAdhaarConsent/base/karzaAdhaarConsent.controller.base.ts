/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { KarzaAdhaarConsentService } from "../karzaAdhaarConsent.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { KarzaAdhaarConsentCreateInput } from "./KarzaAdhaarConsentCreateInput";
import { KarzaAdhaarConsentWhereInput } from "./KarzaAdhaarConsentWhereInput";
import { KarzaAdhaarConsentWhereUniqueInput } from "./KarzaAdhaarConsentWhereUniqueInput";
import { KarzaAdhaarConsentFindManyArgs } from "./KarzaAdhaarConsentFindManyArgs";
import { KarzaAdhaarConsentUpdateInput } from "./KarzaAdhaarConsentUpdateInput";
import { KarzaAdhaarConsent } from "./KarzaAdhaarConsent";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class KarzaAdhaarConsentControllerBase {
  constructor(
    protected readonly service: KarzaAdhaarConsentService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: KarzaAdhaarConsent })
  @nestAccessControl.UseRoles({
    resource: "KarzaAdhaarConsent",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(
    @common.Body() data: KarzaAdhaarConsentCreateInput
  ): Promise<KarzaAdhaarConsent> {
    return await this.service.create({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        reqPayload: true,
        resPayload: true,
        customerId: true,
        mobileNumber: true,
        httpStatusCode: true,
        errorMessage: true,
        errorCode: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [KarzaAdhaarConsent] })
  @ApiNestedQuery(KarzaAdhaarConsentFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "KarzaAdhaarConsent",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(
    @common.Req() request: Request
  ): Promise<KarzaAdhaarConsent[]> {
    const args = plainToClass(KarzaAdhaarConsentFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        reqPayload: true,
        resPayload: true,
        customerId: true,
        mobileNumber: true,
        httpStatusCode: true,
        errorMessage: true,
        errorCode: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: KarzaAdhaarConsent })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "KarzaAdhaarConsent",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: KarzaAdhaarConsentWhereUniqueInput
  ): Promise<KarzaAdhaarConsent | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        reqPayload: true,
        resPayload: true,
        customerId: true,
        mobileNumber: true,
        httpStatusCode: true,
        errorMessage: true,
        errorCode: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: KarzaAdhaarConsent })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "KarzaAdhaarConsent",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: KarzaAdhaarConsentWhereUniqueInput,
    @common.Body() data: KarzaAdhaarConsentUpdateInput
  ): Promise<KarzaAdhaarConsent | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          reqPayload: true,
          resPayload: true,
          customerId: true,
          mobileNumber: true,
          httpStatusCode: true,
          errorMessage: true,
          errorCode: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: KarzaAdhaarConsent })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "KarzaAdhaarConsent",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: KarzaAdhaarConsentWhereUniqueInput
  ): Promise<KarzaAdhaarConsent | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          reqPayload: true,
          resPayload: true,
          customerId: true,
          mobileNumber: true,
          httpStatusCode: true,
          errorMessage: true,
          errorCode: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
