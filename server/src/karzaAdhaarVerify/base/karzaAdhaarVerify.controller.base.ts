/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { KarzaAdhaarVerifyService } from "../karzaAdhaarVerify.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { KarzaAdhaarVerifyCreateInput } from "./KarzaAdhaarVerifyCreateInput";
import { KarzaAdhaarVerifyWhereInput } from "./KarzaAdhaarVerifyWhereInput";
import { KarzaAdhaarVerifyWhereUniqueInput } from "./KarzaAdhaarVerifyWhereUniqueInput";
import { KarzaAdhaarVerifyFindManyArgs } from "./KarzaAdhaarVerifyFindManyArgs";
import { KarzaAdhaarVerifyUpdateInput } from "./KarzaAdhaarVerifyUpdateInput";
import { KarzaAdhaarVerify } from "./KarzaAdhaarVerify";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class KarzaAdhaarVerifyControllerBase {
  constructor(
    protected readonly service: KarzaAdhaarVerifyService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: KarzaAdhaarVerify })
  @nestAccessControl.UseRoles({
    resource: "KarzaAdhaarVerify",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(
    @common.Body() data: KarzaAdhaarVerifyCreateInput
  ): Promise<KarzaAdhaarVerify> {
    return await this.service.create({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        customerId: true,
        adhaarNumber: true,
        reqPayload: true,
        resPayload: true,
        httpStatusCode: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [KarzaAdhaarVerify] })
  @ApiNestedQuery(KarzaAdhaarVerifyFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "KarzaAdhaarVerify",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<KarzaAdhaarVerify[]> {
    const args = plainToClass(KarzaAdhaarVerifyFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        customerId: true,
        adhaarNumber: true,
        reqPayload: true,
        resPayload: true,
        httpStatusCode: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: KarzaAdhaarVerify })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "KarzaAdhaarVerify",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: KarzaAdhaarVerifyWhereUniqueInput
  ): Promise<KarzaAdhaarVerify | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        customerId: true,
        adhaarNumber: true,
        reqPayload: true,
        resPayload: true,
        httpStatusCode: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: KarzaAdhaarVerify })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "KarzaAdhaarVerify",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: KarzaAdhaarVerifyWhereUniqueInput,
    @common.Body() data: KarzaAdhaarVerifyUpdateInput
  ): Promise<KarzaAdhaarVerify | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          customerId: true,
          adhaarNumber: true,
          reqPayload: true,
          resPayload: true,
          httpStatusCode: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: KarzaAdhaarVerify })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "KarzaAdhaarVerify",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: KarzaAdhaarVerifyWhereUniqueInput
  ): Promise<KarzaAdhaarVerify | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          customerId: true,
          adhaarNumber: true,
          reqPayload: true,
          resPayload: true,
          httpStatusCode: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
