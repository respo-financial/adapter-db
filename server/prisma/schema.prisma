datasource postgres {
  provider = "postgresql"
  url      = env("DB_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  firstName String?
  lastName  String?
  username  String   @unique
  password  String
  roles     Json
}

model KarzaPan {
  id             String   @id @default(cuid())
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
  panNumber      String
  reqPayload     Json
  resPayload     Json?
  errorMessage   String?
  httpStatusCode String?
  errorCode      String?
  customerId     Int      @unique
  panName        String?
}

model KarzaEmployment {
  id             String   @id @default(cuid())
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
  reqPayload     Json
  resPayload     Json
  customerId     Int      @unique
  errorCode      String?
  errorMessage   String?
  httpStatusCode String?
}

model HyperVergeAadhaar {
  errorCode        String?
  reqType          String
  hyperVergeStatus String?
  id               String   @id @default(cuid())
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
  errorMessage     String?
  customerId       Int      @unique
  reqPayload       Json
  httpStatusCode   String
  resPayload       Json?
  adhaarNumber     String
}

model KarzaAdhaarVerify {
  id             String   @id @default(cuid())
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
  customerId     Int      @unique
  adhaarNumber   Int      @unique
  reqPayload     Json?
  resPayload     Json?
  httpStatusCode Int?
}

model KarzaVerifyAdhaar {
  errorMessage   String?
  id             String   @id @default(cuid())
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
  customerId     Int      @unique
  adhaarNumber   Int      @unique
  resPayload     Json?
  httpStatusCode Int
  reqPayload     Json
  errorCode      String?
  statusCode     String
  reqType        String
}

model HyperVergeKycFailureHistory {
  customerId     Int?     @unique
  reqPayload     Json?    @unique
  id             String   @id @default(cuid())
  resPayload     Json?    @unique
  httpStatusCode Int      @unique
  errorCode      Int      @unique
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
  errorMessage   String
}

model HyperVergeOtp {
  id             String   @id @default(cuid())
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
  customerId     Int      @unique
  reqPayload     Json
  resPayload     Json?
  httpStatusCode String
  errorCode      String?
  errorMessage   String?
}

model HyperVergeKycSuccess {
  aadhaarNumber Int?      @unique
  aadhaarDob    DateTime?
  panNumber     String?   @unique
  panDob        DateTime?
  resPayload    Json?     @unique
  customerId    Int?      @unique
  id            String    @id @default(cuid())
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  mobileNumber  Int?      @unique
  reqPayload    Json?     @unique
}

model KnowlarityMakeCall {
  id                   String   @id @default(cuid())
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt
  customerId           Int      @unique
  agentId              Int      @unique
  customerMobileNumber Int      @unique
  agentMobileNumber    Int
  reqPayload           Json
  resPayload           Json
  httpStatusCode       String?
  errorCode            String
  errorMessage         String
}

model ExperianCreditBureau {
  resPayload         Json?
  id                 String   @id @default(cuid())
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt
  reqType            String?
  experianStatusCode String?
  httpStatusCode     String?
  errorMessage       String?
  errorCode          String?
  customerId         Int      @unique
  reqPayload         Json
}

model KarzaCustomerDetail {
  errorCode      String?
  errorMessage   String?
  panNumber      String   @unique
  reqPayload     Json?
  httpStatusCode String?
  resPayload     Json?
  customerId     Int      @unique
  id             String   @id @default(cuid())
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
}

model CashfreePaymentGateway {
  reqPayload     Json?
  resPayload     Json
  errorMessage   String
  errorCode      String
  customerId     Int?     @unique
  id             String   @id @default(cuid())
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
  httpStatusCode String?
}
